# Step-by-step project setup

1. Created a server file to run the project on this particular server.
2. app.js file setup for all routes [basic server & folder structure setup]
3. Database setup(MongoDB Atlas) --> config(dbConnection.js) with server setup
4. Schema design, email validation, password hashing using bcrypt, and a method to compare passwords
5. Authentication routes (Register and login)