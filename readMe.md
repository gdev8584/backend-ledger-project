# Step by Step project create

1. created server file to run project on this perticular server.
2. app.js file setup for all routes [basic server & folder structure setup]
3. Database setup(MongoDB Atlas) --> config(dbConnection.js) with server setup
4. Schema Design, email validation, password hashing using bcypt, create method to compare password
5. Authentication routes (Register and login)